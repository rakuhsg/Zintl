[package]
name = "zintl-ui-desktop"
edition = "2024"

[dependencies]
zintl-ui = { path = "../zintl-ui" }
zintl-ui-layout = { path = "../zintl-ui-layout" }

[target.'cfg(target_os = "macos")'.dependencies]
zintl-ui-appkit = { path = "../zintl-ui-appkit" }
