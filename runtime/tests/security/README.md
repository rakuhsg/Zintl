# Security regression tests

Security regressions are added with each milestone. Tests cover deny-by-default
authority, capability and handle forgery, cross-runtime and stale reuse, path
escape, permission envelope tampering, bounded exhaustion, exactly-once races,
and absence of raw native handles.

