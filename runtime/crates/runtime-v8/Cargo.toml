[package]
name = "runtime-v8"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
runtime-engine = { path = "../runtime-engine" }
v8 = { version = "152", default-features = false, features = ["simdutf"] }

[lints.rust]
unsafe_code = "deny"

[lints.clippy]
all = "warn"
pedantic = "warn"
