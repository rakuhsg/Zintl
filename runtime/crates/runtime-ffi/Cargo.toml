[package]
name = "runtime-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
runtime-core = { path = "../runtime-core" }
runtime-event-loop = { path = "../runtime-event-loop" }
runtime-filesystem = { path = "../runtime-filesystem" }
runtime-ops = { path = "../runtime-ops" }
runtime-resource = { path = "../runtime-resource" }

[lints.rust]
unsafe_code = "allow"

[lints.clippy]
all = "warn"
pedantic = "warn"
