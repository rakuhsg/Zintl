[package]
name = "zintl-app"
edition = "2024"
version.workspace = true
license.workspace = true

[dependencies]
futures = "0.3"
zintl-native = { path = "../zintl-native", features = ["wgpu"] }
wgpu = "26"
